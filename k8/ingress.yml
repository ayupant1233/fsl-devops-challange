apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fsl-dev-ingress
  namespace: prod
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: fsl-challenge.me
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fsl-dev-service
            port:
              number: 8080